name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create zip
        run: |
          mkdir -p LeadingTiger9.ReusableKeyCard
          cp -r manifest.json project.json content/ LeadingTiger9.ReusableKeyCard/
          zip -r LeadingTiger9.ReusableKeyCard.zip LeadingTiger9.ReusableKeyCard/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: LeadingTiger9.ReusableKeyCard.zip